[package]
name = "antarc-protocol-old"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
crc32fast = { version = "*", features = ["std", "nightly"] }
log = { version = "*" }
thiserror = "*"
